# Enable dev-only features (e.g. daily puzzle creator at /create-daily)
VITE_DEV_MODE=true

# API base URL: use backend directly so /api/daily works when you run dev:all (server + dev).
# If you only run "npm run dev", start the server too: npm run dev:all
VITE_API_URL=http://localhost:3001
